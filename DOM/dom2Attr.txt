DOM属性：
节点的属性有两种：  1 是普通属性，2是属性节点。
普通属性很好理解，就是你用点语法，或者[] 设置的属性。如：object.age = '13';object['age']= '13';
节点属性。这个属性是拼接到节点上的，用object.setAttribute('key','value') 。
用object.getAttribute('key') 得到节点属性，然后对节点属性操作。
总结：普通属性和节点属性是同级别的，除了几个个别的内敛属性。如：id，title。。等，操作点语法赋值，就相当于给对应名字的属性节点中的value赋值.
